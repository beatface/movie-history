{{!-- {{#each posterListings}}
<div class="col-sm-3 col-xs-12 poster-display-unit search-result">
	<div class="poster" id="{{imdbID}}">
		<img id="{{imdbID}}" class="pull-right" key="{{imdbID}}" src="{{Poster}}" onError="
		this.onerror=null;$(this).parent().html('<h3>{{Title}}</h3>');">
	</div>
	<div class="poster-controls">
		<button id="{{imdbID}}" class="movie-add btn btn-default pull-right">Add</button>
	</div>
</div>
{{/each}} --}}